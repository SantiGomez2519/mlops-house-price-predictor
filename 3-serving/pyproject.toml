[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "house-price-predictor-serving"
version = "0.1.0"
description = "FastAPI service for house price predictions"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "pandas",
    "scikit-learn",
]

[tool.setuptools]
packages = []
